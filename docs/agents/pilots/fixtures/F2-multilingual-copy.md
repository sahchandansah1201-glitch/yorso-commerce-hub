# F2: Multilingual UI copy

Review one compact form with default, helper, validation, empty, loading, success
and destructive states in EN, RU and ES-ES. Detect raw enum leakage, literal
translation, semantic drift, broken plural rules, inaccessible names and 390px
truncation. Require independent locale-review metadata.
