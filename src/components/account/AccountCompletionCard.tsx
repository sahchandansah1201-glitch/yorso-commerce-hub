/**
 * AccountCompletionCard — публичное имя для карточки прогресса заполнения
 * аккаунта (правый сайдбар). Alias над уже существующим `AccountOverview`,
 * чтобы код мог импортировать примитив под именем из спецификации P0A.
 */
export { AccountOverview as AccountCompletionCard } from "./AccountOverview";
