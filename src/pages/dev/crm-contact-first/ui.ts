/**
 * Shared local class constants for the contact-first prototype.
 * The shared Button shrinks to 40px on sm, so the prototype pins 44px
 * on both 390px and 1280px.
 */
export const CONTROL = "!h-[44px] !min-h-[44px] !min-w-[44px] text-sm";
export const ICON_CONTROL = "!h-[44px] !min-h-[44px] !w-[44px] !min-w-[44px] px-0";
/** Native select/input styling matched to the shared design tokens. */
export const FIELD =
  "h-[44px] w-full min-w-0 rounded-md border border-input bg-background px-3 text-sm text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background";
